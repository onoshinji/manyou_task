<h2 class ="text-center">Sign up</h2>
<%# 全ての雛形ができたらエラーメッセージをフォームないに移動させる %>
<% if @user.errors.any? %>
<!--error_explanationをIDに設定するとデフォルトで設定されているスタイルが適用される -->
  <div id="error_explanation">
    <h3><%= pluralize(@user.errors.count, "件のエラーが出たで。") %>すまんな、どこか違うんや。確認してくれるか？</h3>
    <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<div class = "text-center">
<%= form_with(model: @user, local: true) do |f| %>
  <br>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <%= f.label :email %>
  <%= f.email_field :email %>
  <br>
  <%= f.label :password %>
  <%= f.password_field :password %>
  <br>
  <%= f.label :password_confirmation %>
  <%= f.password_field :password_confirmation %>
  <br>
  <button type="button" class = "btn btn-priority">
  <%= f.submit "アカウントを作成する" %>
  </button>
<% end %>
</div>
